# Regression summary

Focused Leave Studio conversation, question, Queensland LSL planner and administrator-context coverage passed `165/165`. This includes the deterministic 7-question × 4-persona matrix, accepted policy-level wording, explicit worker-context boundary, governed universal-age exception, semantic negative cases and exact fallback codes.

A broader 16-file Leave/LSL/Minerva regression selection passed `261/261`. Existing representative coverage for source precedence, HOLD explanations, service thresholds, valuation, evidence support, and field/scenario fallback plus audit remains green.

Python compilation and `git diff --check` passed. This repository has no configured/installed standalone linter or type checker in the shared verification environment. The changed/untracked slice files produced zero credential-pattern findings.

The final fixed live-provider matrix after the residual “rather than age” guard accepted 18 compositions and correctly fell back 10 times. All 28 responses were grounded, carried an audit identity and `NoChangesMade=true`; no unsafe live wording was returned.
